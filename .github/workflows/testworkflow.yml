name: Test reusable workflow on tag

on:
  push:
    tags:
      - '*'

jobs:
  call-central:
    uses: ./.github/workflows/central-docker-build.yml
    with:
      image_name: tag-docker-project
    
