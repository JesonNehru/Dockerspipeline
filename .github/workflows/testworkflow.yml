name: Test reusable workflow on tag

on:
  push:
    tags:
      - '*'

jobs:
  call-central:
    uses: Dockerspipeline/.github/workflows/resuable.yml
    with:
      image_name: tag-docker-project
    
